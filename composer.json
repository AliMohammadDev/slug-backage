{
    "name": "alimohammaddev/slug",
    "description": "Slug creator for Laravel",
    "type": "library",
    "license": "MIT",
    "minimum-stability": "dev",
    "require": {
        "php": ">=8.2",
        "illuminate/database": "^10.0 || ^11.0",
        "illuminate/support": "^10.0 || ^11.0"
    },
    "autoload": {
        "psr-4": {
            "wdd\\slug\\": "src/"
        }
    },
    "extra": {
        "laravel": {
            "providers": [
                "wdd\\slug\\SlugServiceProvider"
            ]
        }
    }
}
